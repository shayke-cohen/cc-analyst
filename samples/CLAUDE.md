# Project Conventions

A sample `CLAUDE.md` that a project might keep in its root for Claude Code to read.

## Tests

- Run `npm test` before committing.
- Use vitest with describe/it.

## Style

- Use TypeScript strict mode.
