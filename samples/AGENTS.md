# Project Conventions for Codex

A sample `AGENTS.md` that a project might keep in its root for Codex to read.

## Working Agreements

- Prefer `rg` over `grep`.
- Run the test suite before suggesting changes are done.

## Build

- `npm run build` for the production bundle.
